services:
  pb_backend:
    build:
      context: .
      dockerfile: Dockerfile.base
      args:
        - PB_VERSION=${PB_VERSION}
    container_name: jxlabs_pb_backend
    volumes:
      - ./pb_data:/pb/pb_data
      - ./pb_hooks.js:/pb/pb_hooks.js
    networks:
      - proxy
    restart: unless-stopped
    environment:
      POCKETBASE_APP_ENV_ADMIN_EMAIL: ${DB_ADMIN_USER}
      POCKETBASE_APP_ENV_ADMIN_PASSWORD: ${DB_ADMIN_PASSWORD}
      POCKETBASE_ENCRYPTION_KEY: ${POCKETBASE_ENCRYPTION_KEY}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.jxlabs_pb_backend.rule=Host(`pocketbase.johnxlabs.de`)"
      - "traefik.http.routers.jxlabs_pb_backend.entrypoints=websecure"
      - "traefik.http.routers.jxlabs_pb_backend.tls.certresolver=letsencrypt"
      - "traefik.http.services.jxlabs_pb_backend.loadbalancer.server.port=8080"

networks:
  proxy:
    external: true